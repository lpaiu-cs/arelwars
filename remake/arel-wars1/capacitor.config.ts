import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.arelwars.recovery',
  appName: 'Arel Wars Recovery',
  webDir: 'dist'
};

export default config;
